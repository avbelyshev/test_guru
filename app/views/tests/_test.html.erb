<tr>
  <td><%= test.title %></td>
  <td><%= test_level(test) %></td>
  <td align="center"><%= test.questions.count %></td>
  <td align="center"><%= button_to t('.start'), start_test_path(test), class: "btn btn-primary btn-sm" %></td>
</tr>
